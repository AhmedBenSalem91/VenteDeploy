# Variables d'orchestration (lues par docker-compose.yml).
# Copier en .env et adapter :  cp .env.example .env

# --- Base de données ---
DB_PASS=change-moi
DB_NAME=shop
DB_SYNC=true                 # false en production

# --- Sécurité backend ---
JWT_SECRET=change-moi-en-prod-openssl-rand-hex-32

# --- Compte admin initial (créé au 1er démarrage) ---
ADMIN_EMAIL=hammaKarmeni@gmail.com
ADMIN_PASSWORD=Karmeni135135

# --- URLs publiques ---
# CORS : origine du frontend autorisée par le backend
CORS_ORIGIN=http://localhost:3001
# URL publique du backend (joignable par le navigateur)
NUXT_PUBLIC_API_BASE=http://localhost:3002
